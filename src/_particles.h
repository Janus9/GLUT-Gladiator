#ifndef _PARTICLES_H
#define _PARTICLES_H

#include <_common.h>

class _particles {
    public:
        _particles();
        virtual ~_particles();
    protected:
    private:
};

#endif // _PARTICLES_H